from setuptools import setup, find_packages

setup(name='assignmentgp',
      version='1.0',
      description='Gaussian process with latent assignment to output dimension',
      author='Karl Tayeb',
      author_email='karl.tayeb7@gmail.com',
      packages=find_packages()
      )